import { notFound } from "next/navigation";
import type { Metadata } from "next";

export const metadata: Metadata = {
    title: "Checkout Alternative - تاوونی",
};

export default function CheckoutAlternative() {
    notFound();
}
